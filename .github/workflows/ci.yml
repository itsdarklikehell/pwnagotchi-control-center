name: CI
on:
  push:
    branches: ["main","main","master"]
  pull_request:
    branches: ["main","main","master"]
  workflow_dispatch:
jobs:
  ci:
    uses: itsdarklikehell/ci-templates/.github/workflows/ci-generic.yml@v1